upstream constructora_web {
    server web:8000;
}

# Servidor principal (constructora.com y www.constructora.com)
server {
    listen 80;
    server_name localhost constructora.com www.constructora.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://constructora_web;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    location = /robots.txt {
        proxy_pass http://constructora_web;
        add_header Content-Type text/plain;
    }

    location = /sitemap.xml {
        proxy_pass http://constructora_web;
        add_header Content-Type application/xml;
    }
}

# Servidor para backoffice (admin.constructora.com)
server {
    listen 80;
    server_name admin.constructora.com;
    client_max_body_size 20M;

    location / {
        proxy_pass http://constructora_web;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }
}
